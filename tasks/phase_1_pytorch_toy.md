# Phase 1 - PyTorch Toy Model

## Objective

Implement baseline Transformer and TinyCore basis model with train/generate loops.

## Inputs

specs/architecture.md, specs/compositional_weights.md, pseudocode/*.py

## Outputs

Trainable tiny model, baseline, generated samples, metrics.

## Acceptance criteria

Both models train on same toy corpus. Loss decreases. Report includes stored unique bytes.

## Notes for Codex

Correctness over speed. Materializing effective weights is allowed initially.
