{
  "name": "tinycore-lm-monorepo",
  "private": true,
  "workspaces": [
    "packages/tinycore-agent",
    "packages/tinycored",
    "packages/tinycore-code"
  ],
  "scripts": {
    "build": "npm --workspaces run build",
    "test": "npm --workspaces run test"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/vscode": "^1.107.0",
    "typescript": "^5.9.3"
  }
}
